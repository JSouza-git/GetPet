<%= stylesheet_link_tag "style_padrao_listar.css" %>
<%= render 'header'  %>

<p id="notice"><%= notice %></p>

<h1 class="title-table">Cargos dos Usuários</h1>
<div class="table-wrapper">
  <table class="fl-table">
    <thead>
      <tr>
        <th>Pet</th>
        <th>Servico</th>
        <th>Usuario</th>
        <th>Data</th>
        <th>Observacao</th>
        <th colspan="3">Ações</th>
      </tr>
    </thead>

    <tbody>
      <% @agendamentos.each do |agendamento| %>
        <tr>
          <td><%= agendamento.pet_id %></td>
          <td><%= agendamento.servico_id %></td>
          <td><%= agendamento.usuario_id %></td>
          <td><%= agendamento.data %></td>
          <td><%= agendamento.observacao %></td>
          <td><%= link_to 'Show', agendamento %></td>
          <td><%= link_to 'Edit', edit_agendamento_path(agendamento) %></td>
          <td><%= link_to 'Destroy', agendamento, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
      <tr>
        <td><%= link_to 'Novo Agendamento', new_agendamento_path %></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
    </tbody>
  </table>
</div>
<%= render 'footer'  %>